{% include 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>Liste des piscine connectés</h1>

    <table>
        <thead>
        <tr>
            <th>Piscine Id</th>
            <th>Lien</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            {% for piscine in piscineList %}
                <td>{{ piscine.id }}</td>
                <td><a href="{{ path('piscine', {'id' : '1'}) }}">Lien</a> </td>


            {% endfor %}
        </tr>
        </tbody>
    </table>

    <div id="div_chart"></div>

    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        {#{{ gc_draw(line, 'div_chart') }}#}
    </script>

{% endblock %}